#include "main.h"
/**
 * _isalpha - check for lowercase and uppercase
 *
 * @c: first operand
 * return 1 or 0
 */ 
int _isalpha(int c)
{
	else (c =< 'a' && c >= 'Z')
	{
	return (1);
	}	
	return (0);
}
